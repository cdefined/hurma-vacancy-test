{
  "name": "hurma-test",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/**/*.html",
    "build": "parcel build src/**/*.html --out-dir build --no-source-maps --public-url ."
  },
  "author": "",
  "browserslist": [
    "last 2 version",
    "ie >= 11",
    "chrome >= 70",
    "safari >= 10"
  ],
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^9.6.0",
    "parcel": "^1.12.3",
    "postcss": "^7.0.17",
    "postcss-preset-env": "^6.6.0"
  }
}
